// Moved to shared domain (pure Port logic, reused by Validation). Re-exported here so the
// existing WorkbenchSDK imports keep working.
export { resolvePorts } from "@pretzel-graph/shared/domain/resolvePorts";
