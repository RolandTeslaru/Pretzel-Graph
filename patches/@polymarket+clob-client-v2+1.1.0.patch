diff --git a/node_modules/@polymarket/clob-client-v2/dist/client.cjs b/node_modules/@polymarket/clob-client-v2/dist/client.cjs
index fd6d479..b069ded 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/client.cjs
+++ b/node_modules/@polymarket/clob-client-v2/dist/client.cjs
@@ -42,8 +42,9 @@ var ClobClient = class {
 	retryOnError;
 	throwOnError;
 	feeSlippage;
-	constructor({ host, chain, signer, creds, signatureType, funderAddress, useServerTime, builderConfig, getSigner, retryOnError, throwOnError, feeSlippage }) {
+	constructor({ host, chain, signer, creds, signatureType, funderAddress, useServerTime, builderConfig, getSigner, retryOnError, throwOnError, feeSlippage, axiosInstance }) {
 		this.host = host.endsWith("/") ? host.slice(0, -1) : host;
+		this.axiosInstance = axiosInstance;
 		this.chainId = chain;
 		if (signer !== void 0) this.signer = signer;
 		if (creds !== void 0) this.creds = creds;
@@ -916,15 +917,15 @@ var ClobClient = class {
 		return result;
 	}
 	async get(endpoint, options, skipThrow = false) {
-		const result = await require_index$2.get(endpoint, options);
+		const result = await require_index$2.get(endpoint, { ...options, axiosInstance: this.axiosInstance });
 		return skipThrow ? result : this.throwIfError(result);
 	}
 	async post(endpoint, options, skipThrow = false) {
-		const result = await require_index$2.post(endpoint, options, this.retryOnError);
+		const result = await require_index$2.post(endpoint, { ...options, axiosInstance: this.axiosInstance }, this.retryOnError);
 		return skipThrow ? result : this.throwIfError(result);
 	}
 	async del(endpoint, options, skipThrow = false) {
-		const result = await require_index$2.del(endpoint, options);
+		const result = await require_index$2.del(endpoint, { ...options, axiosInstance: this.axiosInstance });
 		return skipThrow ? result : this.throwIfError(result);
 	}
 };
diff --git a/node_modules/@polymarket/clob-client-v2/dist/client.d.cts b/node_modules/@polymarket/clob-client-v2/dist/client.d.cts
index 8766bed..ef1f5fb 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/client.d.cts
+++ b/node_modules/@polymarket/clob-client-v2/dist/client.d.cts
@@ -12,6 +12,7 @@ import { adjustBuyAmountForFees } from "./fees/index.cjs";
 //#region src/client.d.ts
 interface ClobClientOptions {
   host: string;
+  axiosInstance?: import("axios").AxiosInstance;
   chain: Chain;
   signer?: ClobSigner;
   creds?: ApiKeyCreds;
@@ -26,6 +27,7 @@ interface ClobClientOptions {
 }
 declare class ClobClient {
   readonly host: string;
+  readonly axiosInstance?: import("axios").AxiosInstance;
   readonly chainId: Chain;
   readonly signer?: ClobSigner;
   readonly creds?: ApiKeyCreds;
diff --git a/node_modules/@polymarket/clob-client-v2/dist/client.d.ts b/node_modules/@polymarket/clob-client-v2/dist/client.d.ts
index f3b416e..e816dd8 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/client.d.ts
+++ b/node_modules/@polymarket/clob-client-v2/dist/client.d.ts
@@ -14,6 +14,7 @@ import { adjustBuyAmountForFees } from "./fees/index.js";
 //#region src/client.d.ts
 interface ClobClientOptions {
   host: string;
+  axiosInstance?: import("axios").AxiosInstance;
   chain: Chain;
   signer?: ClobSigner;
   creds?: ApiKeyCreds;
@@ -28,6 +29,7 @@ interface ClobClientOptions {
 }
 declare class ClobClient {
   readonly host: string;
+  readonly axiosInstance?: import("axios").AxiosInstance;
   readonly chainId: Chain;
   readonly signer?: ClobSigner;
   readonly creds?: ApiKeyCreds;
diff --git a/node_modules/@polymarket/clob-client-v2/dist/client.js b/node_modules/@polymarket/clob-client-v2/dist/client.js
index 06eb6ee..70e0859 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/client.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/client.js
@@ -42,8 +42,9 @@ var ClobClient = class {
 	retryOnError;
 	throwOnError;
 	feeSlippage;
-	constructor({ host, chain, signer, creds, signatureType, funderAddress, useServerTime, builderConfig, getSigner, retryOnError, throwOnError, feeSlippage }) {
+	constructor({ host, chain, signer, creds, signatureType, funderAddress, useServerTime, builderConfig, getSigner, retryOnError, throwOnError, feeSlippage, axiosInstance }) {
 		this.host = host.endsWith("/") ? host.slice(0, -1) : host;
+		this.axiosInstance = axiosInstance;
 		this.chainId = chain;
 		if (signer !== void 0) this.signer = signer;
 		if (creds !== void 0) this.creds = creds;
@@ -916,15 +917,15 @@ var ClobClient = class {
 		return result;
 	}
 	async get(endpoint, options, skipThrow = false) {
-		const result = await get(endpoint, options);
+		const result = await get(endpoint, { ...options, axiosInstance: this.axiosInstance });
 		return skipThrow ? result : this.throwIfError(result);
 	}
 	async post(endpoint, options, skipThrow = false) {
-		const result = await post(endpoint, options, this.retryOnError);
+		const result = await post(endpoint, { ...options, axiosInstance: this.axiosInstance }, this.retryOnError);
 		return skipThrow ? result : this.throwIfError(result);
 	}
 	async del(endpoint, options, skipThrow = false) {
-		const result = await del(endpoint, options);
+		const result = await del(endpoint, { ...options, axiosInstance: this.axiosInstance });
 		return skipThrow ? result : this.throwIfError(result);
 	}
 };
diff --git a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.cjs b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.cjs
index 946edd3..cc2d55b 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.cjs
+++ b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.cjs
@@ -15,9 +15,9 @@ const overloadHeaders = (method, headers = {}) => {
 	headers["Content-Type"] = "application/json";
 	if (method === GET) headers["Accept-Encoding"] = "gzip";
 };
-const request = async (endpoint, method, headers, data, params) => {
+const request = async (endpoint, method, headers, data, params, client) => {
 	overloadHeaders(method, headers);
-	return await (0, axios.default)({
+	return await (client || (0, axios.default))({
 		method,
 		url: endpoint,
 		headers,
@@ -28,12 +28,12 @@ const request = async (endpoint, method, headers, data, params) => {
 const sleep = (ms) => new Promise((res) => setTimeout(res, ms));
 const post = async (endpoint, options, retryOnError = false) => {
 	try {
-		return (await request(endpoint, POST, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, POST, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		if (retryOnError && isTransientAxiosError(err)) {
 			await sleep(30);
 			try {
-				return (await request(endpoint, POST, options?.headers, options?.data, options?.params)).data;
+				return (await request(endpoint, POST, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 			} catch (retryErr) {
 				return errorHandling(retryErr);
 			}
@@ -43,14 +43,14 @@ const post = async (endpoint, options, retryOnError = false) => {
 };
 const get = async (endpoint, options) => {
 	try {
-		return (await request(endpoint, GET, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, GET, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		return errorHandling(err);
 	}
 };
 const del = async (endpoint, options) => {
 	try {
-		return (await request(endpoint, DELETE, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, DELETE, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		return errorHandling(err);
 	}
diff --git a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
index d7e38a4..122ecc9 100644
--- a/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
+++ b/node_modules/@polymarket/clob-client-v2/dist/http-helpers/index.js
@@ -13,9 +13,9 @@ const overloadHeaders = (method, headers = {}) => {
 	headers["Content-Type"] = "application/json";
 	if (method === GET) headers["Accept-Encoding"] = "gzip";
 };
-const request = async (endpoint, method, headers, data, params) => {
+const request = async (endpoint, method, headers, data, params, client = axios) => {
 	overloadHeaders(method, headers);
-	return await axios({
+	return await client({
 		method,
 		url: endpoint,
 		headers,
@@ -26,12 +26,12 @@ const request = async (endpoint, method, headers, data, params) => {
 const sleep = (ms) => new Promise((res) => setTimeout(res, ms));
 const post = async (endpoint, options, retryOnError = false) => {
 	try {
-		return (await request(endpoint, POST, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, POST, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		if (retryOnError && isTransientAxiosError(err)) {
 			await sleep(30);
 			try {
-				return (await request(endpoint, POST, options?.headers, options?.data, options?.params)).data;
+				return (await request(endpoint, POST, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 			} catch (retryErr) {
 				return errorHandling(retryErr);
 			}
@@ -41,14 +41,14 @@ const post = async (endpoint, options, retryOnError = false) => {
 };
 const get = async (endpoint, options) => {
 	try {
-		return (await request(endpoint, GET, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, GET, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		return errorHandling(err);
 	}
 };
 const del = async (endpoint, options) => {
 	try {
-		return (await request(endpoint, DELETE, options?.headers, options?.data, options?.params)).data;
+		return (await request(endpoint, DELETE, options?.headers, options?.data, options?.params, options?.axiosInstance)).data;
 	} catch (err) {
 		return errorHandling(err);
 	}
